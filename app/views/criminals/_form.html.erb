<%= form_for(@criminal) do |f| %>
<% if @criminal.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@criminal.errors.count, "error") %> prohibited this criminal from being saved:</h2>

  <ul>
    <% @criminal.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :photo ,"Select the Photo" %><br />
  <%= f.file_field :photo %>
</div>
<div class="actions">
  <%= f.submit "Submit" %>
</div>
<% end %>
