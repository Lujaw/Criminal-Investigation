<header>
  <hgroup>
    <a href="#" title="Criminal Investigation"><img src="../images/logo.gif" alt="Criminal Investigation" height=100px width=100px/>
    <h2>Criminal Investigation </h2>   </a>
    <h3>Defending Our Nation. Securing The Future </h3>
      <% if user_signed_in?%>
          <div id = "welcome_message"><%=current_user.email%>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
          </div>
          <% end %>
  </hgroup>
</header>